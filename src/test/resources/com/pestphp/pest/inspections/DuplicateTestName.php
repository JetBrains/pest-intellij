<?php

<error descr="Pest test names must be unique within a file">test('basic', function () {
    $this->assertTrue(true);
})</error>;

<error descr="Pest test names must be unique within a file">test('basic', function () {
    $this->assertTrue(true);
})</error>;